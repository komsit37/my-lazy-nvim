return {
  "dstein64/nvim-scrollview",
  -- optional: trigger loading only on certain events for performance
  event = "VeryLazy",
  -- optional config
  config = function()
    -- customize as needed
    -- vim.g.scrollview_base = 'right'
    -- vim.g.scrollview_cursor_symbol = '|'
    -- see :help scrollview-configuration for more options
  end,
}
